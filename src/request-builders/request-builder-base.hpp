// #ifndef REQUEST_BUILDER_BASE_HPP
// #define REQUEST_BUILDER_BASE_HPP

// #include <string>
// #include <map>

// class RequestBuilderBase
// {
// public:
//     virtual RequestBuilderBase& add_header(const std::string& key, const std::string& value) = 0;
// 	virtual RequestBuilderBase& add_headers(const std::map<std::string, std::string>& headers) = 0;
// 	virtual RequestBuilderBase& add_headers(/*Object?*/) = 0;

// 	virtual RequestBuilderBase& add_body_parameter(const std::string& body) = 0;
// };

// #endif //REQUEST_BUILDER_BASE_HPP